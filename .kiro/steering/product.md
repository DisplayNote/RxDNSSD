---
inclusion: always
---

# Product

RxDNSSD — Android DNS-SD (mDNS/Bonjour/Zeroconf) client library, published as three Maven artifacts (`dnssd`, `rxdnssd`, `rx2dnssd`) from one repo, plus a sample app. Archived upstream (see `README.md`); this is a DisplayNote-maintained internal fork.

Full detail: `AGENTS.md` at repo root, `docs/architecture.md`, `docs/glossary.md`.
